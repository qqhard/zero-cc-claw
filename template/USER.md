# User Profile

- **Preferred name**: (how the assistant should address you)
- **Timezone**: (IANA name, e.g. `Asia/Singapore`, `America/Los_Angeles`. Must match the host machine's `TZ` so cron fires at the right local time. If user travels, update this and re-register cron jobs.)
- **Language**: (preferred language)
- **Telegram user_id**: (numeric id)
- **Telegram chat_id**: (numeric id)

## About

(Optional: brief intro — role, interests, work context, anything the assistant should know)
